# Portas (usadas por "npm run dev" para subir API e front juntos)
PORT=3001
FRONTEND_PORT=3000

# URL da API (o frontend usa para proxy de /api). Mesma porta que PORT.
API_URL=http://127.0.0.1:3001

# URL do frontend (usada pela API para redirecionar quem acessa a porta da API no navegador)
FRONTEND_URL=http://127.0.0.1:3000

SESSION_SECRET=supersecretkey

# LDAP Connection
LDAP_URL=ldaps://auth.tiozaodolinux.com
LDAP_BASE_DN=DC=seudominio,Dc=com,DC=br

# Domain suffix for binding (fallback)
LDAP_DOMAIN=seudominio.com.br

# Authenticated Bind (Service Account)
# Required for searching users/groups and performing updates
LDAP_ADMIN_DN=Administrator@seudominio.com.br
LDAP_ADMIN_PASSWORD=SuperSecretPassword@2025

# Group required to access the application
LDAP_GROUP_REQUIRED=CN=ADWEB-Admin,CN=Users,DC=seudominio,DC=com,DC=br

# Group required to exclude users (DELETE). Only members of this group can delete users. If not set, no one can delete.
# LDAP_GROUP_DELETE=CN=ADWEB-Delete,CN=Users,DC=seudominio,DC=com,DC=br

# Debug mode
LDAP_DEBUG=true

# Agendamentos (férias) e logs de auditoria: pasta onde ficam os JSON. Padrão: data/
# scheduled-actions.json e audit-log.json são criados nesta pasta.
# SCHEDULE_DATA_DIR=data

# Atributos extras do AD para buscar e editar (ex.: cpf, employeeID). Sem arquivo de config, esses
# nomes entram em "fetch" e no formulário de edição em "Outros". Veja data/ad-user-attributes.example.json
# para controlar labels e seções (ex.: CPF em "Documentos").
# AD_EXTRA_ATTRIBUTES=cpf
